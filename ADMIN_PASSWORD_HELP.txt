/**
 * SIMPLE ADMIN PASSWORD RESET
 * Paste this in browser console to bypass auth temporarily
 */

// TEMPORARY BYPASS (this session only):
localStorage.setItem('adminAuth', 'true');
window.location.reload();

// Then enter your website admin page URL

// ===== TO SET A PERMANENT PASSWORD =====
// Your password is checked in one of these places:
// 1. Environment variable (ADMIN_PASSWORD)
// 2. Hardcoded in HTML/JS  
// 3. Backend API check

// ASK WHERE YOU HOST IT:
// - Cloudflare Workers? → Check .env or wrangler.toml
// - Vercel? → Environment Variables in dashboard
// - GitHub Pages? → Can't store passwords (static only)
// - Other? → Check your hosting provider's dashboard
